import React from 'react'
import { View } from 'react-native';
import MyText from '../Helper/MyText';

interface Props {
  text:string;
  data:number | [];
}

const AchievementCard: React.FC<Props> = ({text, data}) => {
  return (
    <View>
      
    </View>
  )
}

export default AchievementCard